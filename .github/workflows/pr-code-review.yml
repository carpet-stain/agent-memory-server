name: PR code review (advisory)

# Deliberately NOT a required status check: an LLM reviewer false-positives, and a
# rate-limit/outage must never block a merge. Vendored DIY step from dotfiles (project-starter-template#60).

on:
  pull_request:
    types: [opened, synchronize, labeled]

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      # Advisory only: assume/fetch failure skips the review, never fails the job (infra#220).
      # Skipped when the var is unset; assume stays denied until infra#307 adds this repo.
      - name: Assume OpenRouter SSM read role
        id: aws-creds
        if: vars.AWS_OPENROUTER_ROLE_ARN != ''
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_OPENROUTER_ROLE_ARN }}
          aws-region: us-east-1
      - name: Check for the review API key
        id: guard
        run: |
          # Unset var: skip by design, quiet. Assume/fetch failure: credential gap, warn.
          if [ -z "${{ vars.AWS_OPENROUTER_ROLE_ARN }}" ]; then
            echo "AWS_OPENROUTER_ROLE_ARN not set — advisory review skipped by design."
            echo "enabled=false" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "${{ steps.aws-creds.outcome }}" != "success" ]; then
            echo "::warning::OIDC role assume failed — advisory review skipped."
            echo "enabled=false" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          value="$(aws ssm get-parameter --name /runtime/openrouter-api-key \
            --with-decryption --query Parameter.Value --output text 2>/dev/null)" || value=""
          if [ -z "$value" ]; then
            echo "::warning::openrouter-api-key SSM fetch failed — advisory review skipped."
            echo "enabled=false" >>"$GITHUB_OUTPUT"
          else
            echo "::add-mask::$value"
            echo "OPENAI_API_KEY=$value" >>"$GITHUB_ENV"
            echo "enabled=true" >>"$GITHUB_OUTPUT"
          fi
      - uses: actions/checkout@v7
        if: steps.guard.outputs.enabled == 'true'
      - uses: actions/setup-node@v7
        if: steps.guard.outputs.enabled == 'true'
        with:
          node-version: "20"
      - name: Advisory review
        if: steps.guard.outputs.enabled == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # OPENAI_API_KEY rides $GITHUB_ENV from the guard step (SSM), not a secrets.* reference.
          OPENAI_API_URL: https://openrouter.ai/api/v1/chat/completions
          OPENAI_MODEL: openai/gpt-5.6-sol
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: node scripts/pr-review/run.mjs
